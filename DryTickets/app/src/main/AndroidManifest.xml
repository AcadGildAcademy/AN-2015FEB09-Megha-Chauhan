<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.megha.drytickets">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@drawable/ic_launcher" android:theme="@style/AppTheme" >
        <activity android:name=".First_page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".ServiceHandler"></activity>
        <activity android:name=".Sign_in_mainclass"></activity>
        <activity android:name=".Now_on_sale_firstpage"></activity>
        <activity android:name=".Sign_up_mainclass"></activity>
        <activity android:name=".SingleItemView"></activity>
    </application>

</manifest>
